import { ReemplazarSignoPipe } from './reemplazar-signo.pipe';

describe('ReemplazarSignoPipe', () => {
  it('create an instance', () => {
    const pipe = new ReemplazarSignoPipe();
    expect(pipe).toBeTruthy();
  });
});
